{
  "name": "telegram-noti-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start:db:local": "docker compose --env-file .env.local up -d couchdb-autobookings",
    "start:local": "npx cross-env NODE_ENV=local ts-node src/index.ts",
    "build:docker:development": "docker compose --env-file .env.development build",
    "build:docker:production": "docker compose --env-file .env.production build",
    "start:development": "docker compose --env-file .env.development up -V",
    "start:production": "docker compose --env-file .env.production up"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "dependencies": {
    "dotenv": "^17.3.1",
    "nano": "^11.0.4",
    "node-telegram-bot-api": "^0.67.0"
  },
  "devDependencies": {
    "@types/node-telegram-bot-api": "^0.64.13",
    "cross-env": "^10.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}